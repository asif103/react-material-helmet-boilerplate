import React from 'react';
import { Hero} from '../../components';
import { Typography } from '@mui/material';

const About = () => {
    return (
        <div>
           <Typography variant='h2'>This is about</Typography>
        </div>
    );
};

export default About;